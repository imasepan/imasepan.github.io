# Accepted decisions

## 2026-08-05 — Repository identity

The project is `imasepan/imasepan.github.io`, deployed through GitHub Pages, with remote `main` as the implementation authority.

## 2026-08-05 — Home/Friend remains available

The existing cozy personal website remains a separate Home/Friend experience. Studio must not remove or redesign it.

## 2026-08-05 — Version 1 is the Studio direction

Version 1 is the canonical professional Studio specification.

## 2026-08-05 — Isolated Version 1 worktree

Version 1 implementation begins from an isolated branch/worktree based on `origin/main`; the stale local `main` checkout is preserved.

## 2026-08-05 — Selector and Home route

The root route is the static perspective selector. Home/Friend is preserved at `/home/`, and its shared navigation now points there. The Studio option is intentionally marked in progress until the actual `/studio/` page is implemented, avoiding a dead link.
